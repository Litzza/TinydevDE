import Node2D from "../Node2D.class.js";

export default class Effect extends Node2D {
    
    constructor() {
        super();
    }

    render() {
        // ctx.beginPath();
        // ctx.strokeStyle = "white";
        // ctx.ellipse(point.centerCenter.x, point.centerCenter.z, this.width, this.height, 2 * Math.PI / 360 * 180, 0, 2 * Math.PI);
        // ctx.stroke();
    }

}